<!DOCTYPE html>
<html lang="nl">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}{% endblock %}</title>

    <link rel="icon" href="{{ asset('svg/qualitycheck.svg') }}">

    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="theme-color" content="#ffffff">
    <meta name="description" content="{% block description %}{% endblock %}">
    <meta name="keywords" content="{% block keywords %}{% endblock %}">
    <meta name="author" content="{% block author %}{% endblock %}">
    <meta name="robots" content="noindex, nofollow">

    <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body class="min-h-screen flex flex-col bg-gray-50 text-gray-900">

<!-- NAV -->
<nav class="bg-white border-b border-gray-200">
    <div class="max-w-7xl mx-auto px-4 h-16 flex items-center justify-between">

        <div class="flex items-center gap-3 font-semibold text-lg">
            <img src="{{ asset('svg/qualitycheck.svg') }}" alt="" class="h-7 w-7">
            Quapio Check
        </div>

        <ul class="flex items-center gap-6 text-sm font-medium">
            <li>
                <a
                    href="/"
                    class="text-gray-600 hover:text-gray-900 transition"
                >
                    Home
                </a>
            </li>
        </ul>
    </div>
</nav>

<!-- PAGE CONTENT -->
<main class="flex-1">
    {% block body %}{% endblock %}
</main>

<!-- FOOTER -->
<footer class="bg-white border-t border-gray-200">
    <div class="max-w-7xl mx-auto px-4 py-6 text-center text-sm text-gray-500 space-y-1">
        <p>
            Quapio Check · Personal utility tool
        </p>
        <p class="text-xs text-gray-400">
            Not an official McDonald’s application
        </p>
    </div>
</footer>

</body>
</html>
